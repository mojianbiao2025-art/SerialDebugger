========================================
🔒 修复 Token 安全问题
🔒 Fix Token Security Issue
========================================

问题 / Issue:
GitHub 检测到 git_push.bat 中包含 Personal Access Token，
阻止了推送以保护你的账号安全。

GitHub detected a Personal Access Token in git_push.bat and
blocked the push to protect your account security.

========================================
解决方案（2选1）/ Solution (Choose 1)
========================================

方案 A：允许这个 Token（快速）
--------------------------------
1. 访问 / Visit:
   https://github.com/mojianbiao2025-art/SerialDebugger/security/secret-scanning/unblock-secret/38vyV41NmsYWJ5Lhp9x6ZJJ5YzL

2. 点击 "Allow secret" 按钮
   Click "Allow secret" button

3. 运行 / Run:
   fix_and_push.bat

方案 B：撤销并创建新 Token（推荐）
--------------------------------
1. 撤销旧 token / Revoke old token:
   https://github.com/settings/tokens
   找到并删除旧的 token

2. 创建新 token / Create new token:
   - 点击 "Generate new token (classic)"
   - 勾选 "repo" 权限
   - 复制新 token

3. 运行 / Run:
   fix_and_push.bat
   
4. 输入新 token 作为密码
   Enter new token as password

========================================
后续使用 / Future Use
========================================

✅ 使用 / Use:
   git_push_safe.bat
   
   这个脚本不包含 token，每次推送时手动输入
   This script doesn't contain token, enter manually each time

❌ 不要使用 / Don't use:
   git_push.bat
   
   这个文件已被添加到 .gitignore，不会被推送
   This file is added to .gitignore and won't be pushed

========================================
安全提示 / Security Tips
========================================

✅ 正确做法 / Good practices:
- 使用 git_push_safe.bat（每次输入 token）
- Token 保存在密码管理器中
- 定期更换 token
- 不要将 token 提交到 Git

❌ 错误做法 / Bad practices:
- 不要将 token 保存在代码文件中
- 不要分享包含 token 的文件
- 不要截图包含 token 的内容

========================================
现在开始 / Start Now
========================================

运行 / Run:
  fix_and_push.bat

然后使用 / Then use:
  git_push_safe.bat

========================================
